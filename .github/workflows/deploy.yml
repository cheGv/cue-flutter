name: Deploy Flutter Web to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.41.6

      - name: Install dependencies
        run: flutter pub get

      - name: Build web
        # APP_ENV=prod is REQUIRED: app_config.dart defaults to sandbox, and a
        # --release build without this pin fails to compile (build guard).
        run: flutter build web --release --base-href "/" --dart-define=APP_ENV=prod

      - name: Add CNAME file for custom domain
        run: echo "app.cuerehab.in" > build/web/CNAME

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
          publish_branch: gh-pages
