// Placeholder test — the default counter smoke test was removed when the app
// was rewritten. Add real widget tests here as the app grows.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
